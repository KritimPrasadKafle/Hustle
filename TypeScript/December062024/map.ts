const map = new Map();

map.set("hello", { name: "ram", age: 30, email: "radhe" });

map.set("hello", { name: "ram", age: 30, email: "radhe" });

const user32 = map.get("hello");
console.log(user32);
